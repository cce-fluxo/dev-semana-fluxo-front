{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    "@typescript-eslint/no-explicit-any": "off", // Desativa a regra do ESLint sobre o uso de "any"
    "@typescript-eslint/no-unused-vars": ["warn", { "argsIgnorePattern": "^_" }] // Alerta para variáveis não usadas, mas não falha o build
  }
}
